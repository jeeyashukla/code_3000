The intended users of the repo are the professor and myself.

The security risks could mess up the functionality of the environment if the code was modified or if the code was in the wrong hands, it could pose threats to academic honesty. The repo doesn't have any sensitive or important data, so the security risks are relatively low.

To secure my repo I've made sure there's no sensitive information stored inside (API keys, passwords, etc), a .gitignore file is used, and pull requests are done by collaborators only. To further security, I could make my repo private, but for now there's no sensitive information, so I leave it public.